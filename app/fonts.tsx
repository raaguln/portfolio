import { Inter, Kalam } from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
})

export const kalam = Kalam({
  subsets: ["latin"],
  weight: "700",
  display: "swap",
})
